# Salon Booking Frontend

Simple React + Vite frontend that talks to the existing backend API.

Quick start:

```bash
cd salon-booking-frontend
npm install
npm run dev
```

Set API base via `.env` (copy `.env.example`).
